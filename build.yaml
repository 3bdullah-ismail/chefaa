targets:
  $default:
    builders:
      hive_generator|hive_generator:
        generate_for:
          exclude:
            - "lib/**/widgets/**"
            - "lib/**/pages/**"
            - "lib/**/layout/**"
      injectable_generator|injectable_builder:
        generate_for:
          exclude:
            - "lib/**/widgets/**"
            - "lib/**/pages/**"
            - "lib/**/layout/**"
